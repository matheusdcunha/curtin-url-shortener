services:
  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - "curtin_db:/data/db"
  redis:
    image: redis:7.4.5-alpine3.21
    ports:
      - "${REDIS_PORT}:6379"

volumes:
  curtin_db:
